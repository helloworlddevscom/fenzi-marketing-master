import { createURL, visitAdminPage } from '@wordpress/e2e-test-utils';

describe("HomePage", () => {


  it( 'Verifies the login button is there ', async () => {

    //const pageResponse = await page.goto(  "http://fenzi-marketing.lando/" );
    //expect(pageResponse);
    //visitAdminPage("/")
    //await visitAdminPage( '/' );

  } );

});
